# -*- coding: utf-8 -*-
# vim: ft=yaml
---
podman:
  pkg:
    name: podman
  rootgroup: root
  config: '/etc/podman'
  service:
    name: podman
  subcomponent:
    config: '/etc/podman-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
