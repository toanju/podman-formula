# -*- coding: utf-8 -*-
# vim: ft=yaml
---
podman:
  pkg:
    name: podman
  bin: /usr/bin/podman
  rootgroup: root
  #homedir_prefix: /opt/containers

  containers: {}

  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
