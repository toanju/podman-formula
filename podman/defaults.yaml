# -*- coding: utf-8 -*-
# vim: ft=yaml
---
podman:
  pkg:
    name: podman
  rootgroup: root
  service:
    name: podman
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
